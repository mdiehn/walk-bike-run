# Changelog

## 0.1.0-dev

- Start Phase 1 app scaffold.
- Add Vite, Leaflet, Vitest, Playwright, ESLint, and Prettier.
- Add a basic map shell and route panel placeholder.
- Add first route model helpers and tests.
- Add top-level `VERSION` as the app version source.
- Add `build.js` to sync the version module and create deployable build metadata.
- Update npm scripts so `npm run build` means “build the app artifact,” not just “run Vite directly.”
